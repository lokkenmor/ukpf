import Rebase from 're-base';

let base = Rebase.createClass({
    authDomain: "coachview-race-reportr.firebaseapp.com",
    databaseURL: "https://coachview-race-reportr.firebaseio.com",
});

export default base;
